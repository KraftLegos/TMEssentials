package com.kraftykaleb.cakeessentials.event.player;

import org.bukkit.Bukkit;
import org.bukkit.ChatColor;
import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;

import mkremins.fanciful.FancyMessage;

@SuppressWarnings("unused")
public class PlayerVote implements Listener {
	
	//@EventHandler
    // public void onPlayerVote(VotifierEvent e){
	//	Vote v = e.getVote();
	//	Bukkit.getServer().broadcastMessage(ChatColor.GREEN + v.getUsername() + " just voted at " + v.getServiceName() + "!");
	//	new FancyMessage("Click here to vote for the server!").color(ChatColor.AQUA).style(ChatColor.BOLD).link(v.getAddress()).send(Bukkit.getServer().getOnlinePlayers());;
	//}
	
}
